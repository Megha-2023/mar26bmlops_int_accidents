# Python 3.12 API/runtime dependencies
# The current MLflow-served model behind `@challenger` is stored as UBJ and
# was logged with XGBoost 3.2.0, so keep this aligned with that artifact.

# Core ML + Data
pandas==2.3.3
numpy==2.4.3
scikit-learn==1.8.0
xgboost==3.2.0
joblib==1.5.3

# MLOps / Pipeline
mlflow==3.10.1
dvc[s3]==3.67.0
prometheus-client==0.20.0
python-dotenv==1.2.2

# API
fastapi==0.135.1
uvicorn==0.41.0
wcwidth==0.6.0
Werkzeug==3.1.6
zipp==3.23.0
httpx==0.28.1
evidently==0.7.21

# Testing
pytest==9.0.2
